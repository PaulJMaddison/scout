apiVersion: 1

providers:
  - name: Context Layer
    orgId: 1
    folder: Context Layer
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards

{
  "manifest": {
    "connectorId": "badMetadataConnector",
    "displayName": "Bad Metadata Connector",
    "version": "1.0.0",
    "description": "Manifest with badly shaped connector metadata.",
    "supportedSourceTypes": ["Crm"],
    "requiredConfigFields": [
      { "name": "endpoint", "type": "string", "description": "API endpoint." },
      { "name": "password", "type": "string", "description": "The account password." }
    ],
    "safeMetadataFields": ["displayName"],
    "sampleEntityMappings": [
      {
        "sourceField": "deal_probability",
        "semanticAttribute": "conversionProbability",
        "description": "Maps deal probability."
      }
    ],
    "configurationSchema": {
      "type": "object",
      "required": ["endpoint"],
      "properties": {
        "endpoint": { "type": "string", "description": "API endpoint." }
      }
    },
    "sampleConfiguration": {
      "endpoint": "https://api.example.com/v1"
    }
  },
  "sampleRecords": [
    {
      "externalUserId": "user-001",
      "observedAtUtc": "2026-06-01T10:00:00Z",
      "payload": { "deal_probability": 0.7 }
    }
  ]
}

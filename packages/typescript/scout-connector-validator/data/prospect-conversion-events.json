{
  "connectorId": "prospectConversionEvents",
  "displayName": "Prospect Conversion Events Metadata",
  "version": "1.0.0",
  "description": "Synthetic prospect conversion-event metadata manifest for validating event taxonomy discovery.",
  "supportedSourceTypes": ["EventStream"],
  "requiredConfigFields": [
    {
      "name": "metadataEndpoint",
      "type": "string",
      "description": "HTTPS endpoint that exposes conversion event taxonomy metadata only."
    },
    {
      "name": "eventTaxonomy",
      "type": "string",
      "description": "Name of the synthetic event taxonomy reviewed for Discovery Signature generation."
    }
  ],
  "safeMetadataFields": [
    "connectorId",
    "displayName",
    "version",
    "supportedSourceTypes",
    "safeMetadataFields",
    "sampleEntityMappings",
    "eventShape"
  ],
  "sampleEntityMappings": [
    {
      "sourceField": "conversion_point_label",
      "semanticAttribute": "conversionProbability",
      "description": "Maps conversion point labels to a synthetic conversion signal."
    },
    {
      "sourceField": "budget_signal_band",
      "semanticAttribute": "budgetReadiness",
      "description": "Maps event taxonomy metadata to a synthetic budget-readiness band."
    },
    {
      "sourceField": "expansion_signal_band",
      "semanticAttribute": "expansionPotential",
      "description": "Maps conversion event metadata to a synthetic expansion-potential signal."
    }
  ],
  "aliases": ["prospectConversions"],
  "capabilities": ["Preview", "DryRun", "ConfigurationValidation", "HealthCheck"],
  "configurationSchema": {
    "type": "object",
    "required": ["metadataEndpoint", "eventTaxonomy"],
    "properties": {
      "metadataEndpoint": {
        "type": "string",
        "description": "HTTPS endpoint for event taxonomy metadata."
      },
      "eventTaxonomy": {
        "type": "string",
        "description": "Synthetic event taxonomy identifier."
      },
      "includeEventNames": {
        "type": "boolean",
        "description": "Whether to include approved event names in the metadata summary.",
        "default": true
      }
    }
  },
  "sampleConfiguration": {
    "metadataEndpoint": "https://metadata.example.com/conversion-events",
    "eventTaxonomy": "synthetic-conversion-taxonomy",
    "includeEventNames": true
  },
  "eventShape": {
    "sourceSystem": "prospectConversionEvents",
    "entityType": "conversionMetadata",
    "sourceIdField": "metadataSourceId",
    "timestampField": "observedAtUtc",
    "payloadRoot": "metadata"
  }
}

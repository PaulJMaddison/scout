{
  "connectorId": "badAuthConnector",
  "displayName": "Bad Auth Connector",
  "version": "1.0.0",
  "description": "Manifest with invalid auth configuration.",
  "supportedSourceTypes": ["Crm"],
  "requiredConfigFields": [
    { "name": "endpoint", "type": "string", "description": "API endpoint." }
  ],
  "safeMetadataFields": ["displayName"],
  "sampleEntityMappings": [
    {
      "sourceField": "deal_probability",
      "semanticAttribute": "conversionProbability",
      "description": "Maps deal probability."
    }
  ],
  "authConfig": {
    "type": "unknownAuthType",
    "scopes": ["read", "write", "read"],
    "tokenUrl": "not-a-url",
    "authoriseUrl": "http://insecure.example.com/auth"
  }
}

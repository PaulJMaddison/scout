{
  "connectorId": "malformedUrlConnector",
  "displayName": "Malformed URL Connector",
  "version": "1.0.0",
  "description": "Manifest with malformed URLs in sample configuration.",
  "supportedSourceTypes": ["Crm"],
  "requiredConfigFields": [
    { "name": "endpoint", "type": "string", "description": "API endpoint." }
  ],
  "safeMetadataFields": ["displayName"],
  "sampleEntityMappings": [
    {
      "sourceField": "deal_probability",
      "semanticAttribute": "conversionProbability",
      "description": "Maps deal probability."
    }
  ],
  "configurationSchema": {
    "type": "object",
    "required": ["endpoint"],
    "properties": {
      "endpoint": { "type": "string", "description": "API endpoint." }
    }
  },
  "sampleConfiguration": {
    "endpoint": "http://insecure-endpoint.example.com/api"
  }
}
